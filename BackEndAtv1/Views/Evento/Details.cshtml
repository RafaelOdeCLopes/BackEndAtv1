@model BackEndAtv1.Models.Evento

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Evento</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Horas)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Horas) horas
        </dd>
        <dt class="col-sm-2">
            Participantes
        </dt>
        <dd class="col-sm-10">
            @if (Model.Participantes != null && Model.Participantes.Any())
            {
                <ul class="list-unstyled">
                    @foreach (var participante in Model.Participantes)
                    {
                        <li>@Html.DisplayFor(modelItem => participante.Nome)</li>
                    }
                </ul>
            }
            else
            {
                <p>Nenhum participante registrado.</p>
            }
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.EventoID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
